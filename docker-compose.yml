version: '3.8'

services:
  mongo:
    image: mongo:7
    container_name: portfolio-db-local
    restart: always
    ports:
      - "27017:27017"
    volumes:
      - mongodata:/data/db

volumes:
  mongodata:
